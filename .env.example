# DeepSeek API Key（可选）
# 若配置了该环境变量，会覆盖代码中的混淆 Key，更安全。
#
# 本地开发：复制本文件为 .env.local 并填入你的 Key
#   VITE_DEEPSEEK_API_KEY=sk-xxxxxxxx
#
# 部署到 GitHub Pages：在仓库
#   Settings → Secrets and variables → Actions
# 添加名为 DEEPSEEK_API_KEY 的 Secret，GitHub Actions 会自动注入。
VITE_DEEPSEEK_API_KEY=
